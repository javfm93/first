<%= form_for(prooftable) do |f| %>
  <% if prooftable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prooftable.errors.count, "error") %> prohibited this prooftable from being saved:</h2>

      <ul>
      <% prooftable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :table %>
    <%= f.text_field :table %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
